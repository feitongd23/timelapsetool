# 延时摄影自动化工具 + AI 照片筛选 — 设计文档

**日期**：2026-06-02
**平台**：macOS 桌面应用
**技术栈**：Electron（前端）+ Python/FastAPI（后端）

---

## 1. 项目概述

一个 macOS 桌面应用，包含两个独立功能：

1. **延时摄影自动流水线**：自动完成 BR → LRT → AE → PR 流程。用户只需在 LRTimelapse 中手动调整关键帧参数，其余全部自动完成。
2. **AI 照片筛选**：拍摄单张/人像时，从一批照片中自动挑选最佳时刻、最佳构图、最佳瞬间。

两个功能独立，互不依赖。

---

## 2. 流程定义（BR-LRT-AE-PR）

| 缩写 | 软件 | 作用 |
|------|------|------|
| **BR** | Adobe Bridge + Camera Raw | RAW 文件初始批处理 |
| **LRT** | LRTimelapse | 关键帧设置、Holy Grail 日转夜过渡、去闪烁 |
| **AE** | After Effects | 图像序列导入、防抖、渲染中间视频 |
| **PR** | Premiere Pro | 最终剪辑、增稳、调色、按规格导出 |

**自动化边界**：用户唯一的手动介入点是在 LRT 中调整关键帧的曝光/色彩参数。其余所有步骤自动完成。

---

## 3. 架构

```
┌─────────────────────────────────────────────┐
│           Electron 前端 (Mac 桌面)            │
│  ┌─────────────────┐  ┌────────────────────┐ │
│  │  延时流水线 Tab   │  │  照片筛选 Tab       │ │
│  │ • 文件夹选择     │  │ • 批量导入          │ │
│  │ • 流程进度看板   │  │ • AI 评分结果展示   │ │
│  │ • 导出参数配置   │  │ • 最佳照片标记      │ │
│  └─────────────────┘  └────────────────────┘ │
└──────────────┬──────────────────┬────────────┘
               │  HTTP/WebSocket  │
┌──────────────▼──────────────────▼────────────┐
│           Python 后端 (FastAPI)               │
│  ┌──────────────────┐  ┌──────────────────┐  │
│  │  流水线自动化引擎  │  │   AI 照片分析引擎  │  │
│  │ • BR 脚本        │  │ • 清晰度评分      │  │
│  │ • LRT CLI 调用   │  │ • 人脸/表情检测   │  │
│  │ • aerender 渲染  │  │ • 构图评分        │  │
│  │ • PR 脚本导出    │  │ • 美学综合评分    │  │
│  └──────────────────┘  └──────────────────┘  │
└──────────────────────────────────────────────┘
               ↓                    ↓
    LRTimelapse / AE / PR      本地照片文件
```

**前后端通信**：HTTP（请求/响应）+ WebSocket（实时进度推送）。

---

## 4. 功能模块

### 4.1 延时流水线控制面板

| 参数 | 控件类型 | 说明 |
|------|----------|------|
| RAW 文件夹 | 路径选择器 | 输入素材 |
| 相机机型 | 下拉（可自定义添加） | 决定可选导出分辨率 |
| 关键帧数量 | 数字输入 (1-20) | LRT 关键帧向导参数，默认 3 |
| 去闪幅度 | 滑块 (0-100) | LRT deflicker strength |
| 去闪遍数 | 数字输入 (1-5) | LRT deflicker passes |
| PR 增稳 | 开关 | Premiere Warp Stabilizer |
| 分辨率 | 下拉（随机型动态填充） | 导出规格 |
| 帧率 | 下拉 (24/25/30/60fps) | 导出规格 |
| 编码 | 下拉 (H.264/H.265/ProRes) | 导出规格 |
| 输出路径 | 路径选择器 | 成片保存位置 |

**交互流程**：
1. 用户选择 RAW 文件夹并配置参数 → 工具自动完成 BR 预处理
2. **暂停点**：工具自动打开 LRTimelapse，用户手动调关键帧参数，调完点"继续"
3. 工具接管：自动去闪烁（指定幅度 × 遍数）→ aerender 渲染图像序列 → PR 合成导出

### 4.2 相机机型与分辨率

机型选择器，选完后分辨率下拉自动填充该机型可选规格。预设示例：

| 相机 | 原生分辨率 | 可选导出分辨率 |
|------|-----------|--------------|
| Sony A7R IV | 9504 × 6336 | 原分辨率 / 8K / 4K / 1080p |
| Sony A7R V | 9728 × 6656 | 原分辨率 / 8K / 4K / 1080p |
| Sony A7 IV | 7008 × 4672 | 原分辨率 / 4K / 1080p |
| Canon R5 | 8192 × 5464 | 原分辨率 / 8K / 4K / 1080p |
| Nikon Z9 | 8256 × 5504 | 原分辨率 / 8K / 4K / 1080p |
| 富士 GFX100S | 11648 × 8736 | 原分辨率 / 8K / 4K / 1080p |

**用户可自定义添加机型**（输入机型名 + 原生分辨率），配置持久化到 `cameras.json`。

通用分辨率规格：
- 8K：7680 × 4320
- 4K：3840 × 2160
- 2K：2048 × 1080
- 1080p：1920 × 1080
- 原分辨率：跟随机型

### 4.3 照片筛选面板

输入：支持两种来源
- A) 一次连拍序列（如 100 张里挑最好的几张）
- B) 一整天/一次外拍的所有照片（跨场景混合）

| AI 评分维度 | 技术实现 |
|------------|---------|
| 清晰度 | Laplacian 方差（OpenCV） |
| 人脸质量 | MediaPipe 检测 + 眼睛开合/表情评分 |
| 构图 | 三分法、主体居中/偏移检测 |
| 综合美学 | NIMA 美学评分模型 |

**结果展示**：网格缩略图，评分最高的照片打金色标记。用户可一键复制/移动最佳照片到新文件夹。

---

## 5. 数据流（延时任务）

```
RAW 文件夹
    │
    ▼ [BR 阶段] Python 调用 Bridge/ACR 批处理
    │  输出：处理后的 RAW/DNG 文件
    │
    ▼ [LRT 阶段 - 上半段] Python 写入关键帧参数到 XMP
    │  → 打开 LRTimelapse，等待用户调参 → 用户点"继续"
    │
    ▼ [LRT 阶段 - 下半段] Python 调用 LRT CLI
    │  去闪烁（幅度 × 遍数） → 导出图像序列（TIFF/JPG）
    │
    ▼ [AE 阶段] aerender 后台渲染
    │  图像序列 → 中间视频（ProRes/DNxHD）
    │
    ▼ [PR 阶段] Premiere ExtendScript
    │  导入 → 可选增稳 → 设置导出规格 → 输出成片
    │
    最终 MP4/MOV 文件
```

---

## 6. 项目文件结构

```
timelapse-tool/
├── electron/          # 前端
│   ├── main.js        # Electron 主进程
│   ├── renderer/      # UI 页面
│   │   ├── pipeline.html   # 延时流水线
│   │   └── selector.html   # 照片筛选
├── python/            # 后端
│   ├── server.py      # FastAPI 入口
│   ├── pipeline/      # 流水线自动化
│   │   ├── br.py
│   │   ├── lrt.py
│   │   ├── ae.py
│   │   └── pr.py
│   ├── ai/            # 照片筛选 AI
│   │   ├── sharpness.py
│   │   ├── face.py
│   │   ├── composition.py
│   │   └── aesthetic.py
│   └── cameras.json   # 相机机型配置
└── scripts/           # AE/PR 脚本
    ├── ae_render.jsx
    └── pr_export.jsx
```

---

## 7. 错误处理

| 失败场景 | 处理策略 |
|---------|---------|
| 软件未安装（LRT/AE/PR） | 启动前预检测，缺失则明确提示并阻止开始 |
| 某阶段崩溃 | 保留中间产物，标记失败阶段，支持**断点续跑** |
| 用户调参阶段超时 | 不超时，无限等待用户点"继续" |
| RAW 文件损坏/格式不支持 | BR 阶段校验，列出问题文件，让用户选择跳过或中止 |
| 渲染磁盘空间不足 | 开始前估算所需空间并检查 |
| AI 模型加载失败 | 照片筛选降级——仍可用基础清晰度评分，跳过美学模型 |

**进度反馈**：每个阶段通过 WebSocket 实时推送进度（百分比 + 当前操作日志）到前端看板。

---

## 8. 测试策略

- **AI 评分模块**：单元测试，准备已知质量的测试图片集（清晰/模糊、睁眼/闭眼、好/差构图），验证评分排序符合预期。
- **流水线各阶段**：mock 外部软件调用，验证参数正确传递、XMP 写入正确、断点续跑逻辑。
- **集成测试**：用一小组真实 RAW 文件跑完整流水线，人工验证成片。
- **前后端通信**：测试 WebSocket 进度推送、错误传播。

---

## 9. 范围边界（YAGNI）

- 仅 macOS，不做 Windows
- 不做拍摄前的气象/光线预测（用户明确表示与延时无关）
- 不做云端/协作功能，纯本地工具
- 照片筛选只做"挑选+标记+复制移动"，不做修图
