# AE 渲染 (After Effects Render) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 AE 阶段：LRT 导出图像序列后，工具用 ExtendScript 新建 AE 工程、导入序列、按表单帧率建合成、加入渲染队列，再用 `aerender` 渲染为 ProRes 4444 中间视频，供 PR 阶段消费。

**Architecture:** 新增 `pipeline/ae.py`，负责（1）定位 AE/aerender，（2）在序列文件夹找到序列锚点文件，（3）生成构建工程的 .jsx 文本，（4）拼装 aerender 命令。`AEStage` 通过依赖注入（可替换的命令执行器 + 路径）调用「AE 跑 jsx 建工程并保存 → aerender 渲染」两步，把中间视频写到由 `output_path` 派生的确定路径 `<output_path>/_ae_intermediate.mov`，PR 阶段（2e）读同一路径。脚本构造与命令拼装全部可单测；真实 AE 调用靠实机验证。

**Tech Stack:** Python 3.9（subprocess 调 AE/aerender + ExtendScript），pytest。After Effects 2026（`/Applications/Adobe After Effects 2026/`）。

---

## File Structure

```
timelapse-tool/python/
├── pipeline/
│   ├── ae.py             # 新增：AE 路径、序列锚点、jsx 生成、aerender 命令、AE 执行编排
│   └── stages.py         # 修改：AEStage.run 调用 ae.py（保留 manual=False）
└── tests/
    ├── test_ae.py        # 新增：纯函数（jsx/命令/锚点）+ 注入假执行器的编排测试
    └── test_stages.py    # 修改：AE 桩测试改为校验委托给 ae 编排
```

**职责边界：** `ae.py` 把「跟 AE 打交道」的全部细节收在一处；`stages.py` 的 `AEStage` 只是把 `config` 转成 `ae.render_sequence(...)` 的调用并 emit 进度。`render_sequence` 接受注入的 `run`（默认 `subprocess.run`）与路径，便于测试。

测试约定：`cd timelapse-tool/python && .venv/bin/python -m pytest`。

---

## 关键约定

- **中间视频路径**：`Path(config.output_path) / "_ae_intermediate.mov"`（确定路径，PR 阶段据此读取）。
- **序列锚点**：AE 导入图像序列时只需指向序列里第一个文件。锚点 = 文件夹内扩展名属于 `.jpg/.jpeg/.tif/.tiff` 的、按文件名排序的第一个文件。
- **帧率**：`config.fps`。
- **ProRes 输出**：aerender 用 `-OMtemplate "Apple ProRes 4444"`。> 实机验证点：该模板名需在 AE 2026 上确认；若不存在需在 AE「输出模块模板」里创建同名模板，或改用实际存在的模板名。计划先用此名，实机迭代。
- **AE 路径**：
  - aerender：`/Applications/Adobe After Effects 2026/aerender`
  - AE 主程序：`/Applications/Adobe After Effects 2026/Adobe After Effects 2026.app/Contents/MacOS/After Effects`

---

## Task 1: AE 路径与序列锚点

**Files:**
- Create: `timelapse-tool/python/pipeline/ae.py`
- Test: `timelapse-tool/python/tests/test_ae.py`

- [ ] **Step 1: 写失败的测试**

创建 `timelapse-tool/python/tests/test_ae.py`：

```python
import pytest

from pipeline import ae


def test_find_sequence_anchor_returns_first_sorted_image(tmp_path):
    (tmp_path / "seq_0003.jpg").write_text("c")
    (tmp_path / "seq_0001.jpg").write_text("a")
    (tmp_path / "seq_0002.jpg").write_text("b")
    (tmp_path / "notes.txt").write_text("x")
    anchor = ae.find_sequence_anchor(tmp_path)
    assert anchor.name == "seq_0001.jpg"


def test_find_sequence_anchor_accepts_tiff(tmp_path):
    (tmp_path / "0001.tif").write_text("a")
    assert ae.find_sequence_anchor(tmp_path).name == "0001.tif"


def test_find_sequence_anchor_no_images_raises(tmp_path):
    (tmp_path / "readme.txt").write_text("x")
    with pytest.raises(ValueError, match="序列"):
        ae.find_sequence_anchor(tmp_path)


def test_intermediate_path_derived_from_output(tmp_path):
    p = ae.intermediate_path(str(tmp_path))
    assert p.name == "_ae_intermediate.mov"
    assert p.parent == tmp_path
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd timelapse-tool/python && .venv/bin/python -m pytest tests/test_ae.py -v`
Expected: FAIL，`ModuleNotFoundError: No module named 'pipeline.ae'`。

- [ ] **Step 3: 写最小实现**

创建 `timelapse-tool/python/pipeline/ae.py`：

```python
from pathlib import Path

SEQUENCE_EXTS = {".jpg", ".jpeg", ".tif", ".tiff"}

AERENDER = "/Applications/Adobe After Effects 2026/aerender"
AE_APP = "/Applications/Adobe After Effects 2026/Adobe After Effects 2026.app/Contents/MacOS/After Effects"

INTERMEDIATE_NAME = "_ae_intermediate.mov"
PRORES_OM_TEMPLATE = "Apple ProRes 4444"
COMP_NAME = "Timelapse"


def find_sequence_anchor(folder):
    """返回序列里排序最靠前的图片文件（AE 导入序列的锚点）。"""
    images = sorted(
        p for p in Path(folder).iterdir()
        if p.suffix.lower() in SEQUENCE_EXTS
    )
    if not images:
        raise ValueError(f"序列文件夹里没有图片: {folder}")
    return images[0]


def intermediate_path(output_dir):
    return Path(output_dir) / INTERMEDIATE_NAME
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd timelapse-tool/python && .venv/bin/python -m pytest tests/test_ae.py -v`
Expected: 4 个测试全部 PASS。

- [ ] **Step 5: 提交**

```bash
git add timelapse-tool/python/pipeline/ae.py timelapse-tool/python/tests/test_ae.py
git commit -m "feat: AE 序列锚点与中间视频路径"
```

---

## Task 2: jsx 生成与 aerender 命令拼装

**Files:**
- Modify: `timelapse-tool/python/pipeline/ae.py`
- Test: `timelapse-tool/python/tests/test_ae.py`

- [ ] **Step 1: 写失败的测试**

在 `timelapse-tool/python/tests/test_ae.py` 末尾追加：

```python
def test_build_ae_script_contains_paths_and_fps():
    jsx = ae.build_ae_script(
        anchor_file="/seq/0001.jpg",
        fps=30,
        project_save_path="/tmp/proj.aep",
    )
    assert "/seq/0001.jpg" in jsx
    assert "/tmp/proj.aep" in jsx
    assert "30" in jsx                 # 帧率写进脚本
    assert "Timelapse" in jsx          # 合成名
    assert "sequence" in jsx.lower()   # 以序列方式导入


def test_build_aerender_cmd_args():
    cmd = ae.build_aerender_cmd(
        aerender="/x/aerender",
        project_path="/tmp/proj.aep",
        output_path="/out/_ae_intermediate.mov",
    )
    assert cmd[0] == "/x/aerender"
    assert "-project" in cmd and "/tmp/proj.aep" in cmd
    assert "-comp" in cmd and "Timelapse" in cmd
    assert "-output" in cmd and "/out/_ae_intermediate.mov" in cmd
    assert "-OMtemplate" in cmd and "Apple ProRes 4444" in cmd
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd timelapse-tool/python && .venv/bin/python -m pytest tests/test_ae.py -v`
Expected: FAIL（`build_ae_script` / `build_aerender_cmd` 未定义）。

- [ ] **Step 3: 写实现**

在 `timelapse-tool/python/pipeline/ae.py` 末尾追加：

```python
def build_ae_script(anchor_file, fps, project_save_path):
    """生成构建 AE 工程的 ExtendScript：导入序列→建合成→入渲染队列→保存。

    渲染由后续 aerender 执行；这里只搭好工程并保存。
    """
    # ExtendScript 里用正斜杠路径即可（macOS）
    return f"""
var anchor = new File("{anchor_file}");
var io = new ImportOptions(anchor);
if (io.canImportAs(ImportAsType.FOOTAGE)) {{
    io.importAs = ImportAsType.FOOTAGE;
}}
io.sequence = true;                       // 以图像序列方式导入
var footage = app.project.importFile(io);
footage.mainSource.conformFrameRate = {fps};

var comp = app.project.items.addComp(
    "{COMP_NAME}",
    footage.width,
    footage.height,
    footage.pixelAspect,
    footage.duration,
    {fps}
);
comp.layers.add(footage);
app.project.renderQueue.items.add(comp);   // 入队，输出模块/路径由 aerender 指定

app.project.save(new File("{project_save_path}"));
app.quit();
""".strip()


def build_aerender_cmd(aerender, project_path, output_path):
    return [
        aerender,
        "-project", project_path,
        "-comp", COMP_NAME,
        "-OMtemplate", PRORES_OM_TEMPLATE,
        "-output", output_path,
    ]
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd timelapse-tool/python && .venv/bin/python -m pytest tests/test_ae.py -v`
Expected: 6 个测试全部 PASS。

- [ ] **Step 5: 提交**

```bash
git add timelapse-tool/python/pipeline/ae.py timelapse-tool/python/tests/test_ae.py
git commit -m "feat: AE 工程 jsx 生成与 aerender 命令拼装"
```

---

## Task 3: 渲染编排（依赖注入，可单测）

**Files:**
- Modify: `timelapse-tool/python/pipeline/ae.py`
- Test: `timelapse-tool/python/tests/test_ae.py`

`render_sequence` 编排：找锚点 → 写 jsx 到临时文件 → 用 AE 跑 jsx 建工程并保存 → aerender 渲染 → 校验中间视频已生成。两次外部调用通过注入的 `run` 执行，便于测试。

- [ ] **Step 1: 写失败的测试**

在 `timelapse-tool/python/tests/test_ae.py` 末尾追加：

```python
def test_render_sequence_invokes_ae_then_aerender(tmp_path):
    seq = tmp_path / "seq"; seq.mkdir(); (seq / "0001.jpg").write_text("i")
    out = tmp_path / "out"; out.mkdir()
    calls = []

    def fake_run(cmd, **kwargs):
        calls.append(cmd)
        # 第二次调用（aerender）后“生成”中间视频
        if cmd[0].endswith("aerender"):
            ae.intermediate_path(str(out)).write_text("video")
        class R: returncode = 0
        return R()

    result = ae.render_sequence(
        seq_folder=str(seq),
        output_dir=str(out),
        fps=24,
        emit=lambda m: None,
        run=fake_run,
        aerender="/x/aerender",
        ae_app="/x/AE",
    )
    assert result == ae.intermediate_path(str(out))
    assert result.exists()
    # 先 AE 建工程，后 aerender
    assert "/x/AE" in calls[0]
    assert calls[1][0] == "/x/aerender"


def test_render_sequence_missing_output_raises(tmp_path):
    seq = tmp_path / "seq"; seq.mkdir(); (seq / "0001.jpg").write_text("i")
    out = tmp_path / "out"; out.mkdir()

    def fake_run(cmd, **kwargs):
        class R: returncode = 0
        return R()  # 不生成中间视频

    with pytest.raises(RuntimeError, match="中间视频"):
        ae.render_sequence(
            seq_folder=str(seq), output_dir=str(out), fps=24,
            emit=lambda m: None, run=fake_run,
            aerender="/x/aerender", ae_app="/x/AE",
        )


def test_render_sequence_nonzero_returncode_raises(tmp_path):
    seq = tmp_path / "seq"; seq.mkdir(); (seq / "0001.jpg").write_text("i")
    out = tmp_path / "out"; out.mkdir()

    def fake_run(cmd, **kwargs):
        class R: returncode = 1
        return R()

    with pytest.raises(RuntimeError, match="失败"):
        ae.render_sequence(
            seq_folder=str(seq), output_dir=str(out), fps=24,
            emit=lambda m: None, run=fake_run,
            aerender="/x/aerender", ae_app="/x/AE",
        )
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd timelapse-tool/python && .venv/bin/python -m pytest tests/test_ae.py -v`
Expected: FAIL（`render_sequence` 未定义）。

- [ ] **Step 3: 写实现**

在 `timelapse-tool/python/pipeline/ae.py` 顶部 import 区加入：

```python
import subprocess
import tempfile
```

在文件末尾追加：

```python
def render_sequence(seq_folder, output_dir, fps, emit, run=subprocess.run,
                    aerender=AERENDER, ae_app=AE_APP):
    """跑完整 AE 阶段，返回中间视频路径。

    run: 可注入的命令执行器（默认 subprocess.run），签名 run(cmd, **kwargs) -> 有 returncode 的对象。
    """
    anchor = find_sequence_anchor(seq_folder)
    out_video = intermediate_path(output_dir)
    proj_path = str(Path(tempfile.gettempdir()) / "timelapse_ae_project.aep")

    # 1) AE 跑 jsx：建工程并保存
    emit("AE 阶段：新建工程并导入图像序列…")
    jsx = build_ae_script(str(anchor), fps, proj_path)
    with tempfile.NamedTemporaryFile("w", suffix=".jsx", delete=False) as f:
        f.write(jsx)
        jsx_path = f.name
    r1 = run([ae_app, "-r", jsx_path])
    if getattr(r1, "returncode", 0) != 0:
        raise RuntimeError("AE 建工程失败")

    # 2) aerender 渲染为 ProRes 4444 中间视频
    emit("AE 阶段：aerender 渲染中间视频（ProRes 4444）…")
    cmd = build_aerender_cmd(aerender, proj_path, str(out_video))
    r2 = run(cmd)
    if getattr(r2, "returncode", 0) != 0:
        raise RuntimeError("aerender 渲染失败")

    if not out_video.exists():
        raise RuntimeError(f"未生成中间视频: {out_video}")
    emit("AE 阶段：完成")
    return out_video
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd timelapse-tool/python && .venv/bin/python -m pytest tests/test_ae.py -v`
Expected: 9 个测试全部 PASS。

- [ ] **Step 5: 提交**

```bash
git add timelapse-tool/python/pipeline/ae.py timelapse-tool/python/tests/test_ae.py
git commit -m "feat: AE 渲染编排（AE 建工程 + aerender，依赖注入可测）"
```

---

## Task 4: AEStage 接入真实渲染

**Files:**
- Modify: `timelapse-tool/python/pipeline/stages.py`
- Modify: `timelapse-tool/python/tests/test_stages.py`

- [ ] **Step 1: 改测试**

编辑 `timelapse-tool/python/tests/test_stages.py`：把现有的 `test_ae_and_pr_stages_emit_progress` 替换为：

```python
def test_ae_stage_delegates_to_render(monkeypatch, tmp_path):
    from pipeline import ae
    called = {}

    def fake_render(seq_folder, output_dir, fps, emit, **kwargs):
        called["seq"] = seq_folder
        called["out"] = output_dir
        called["fps"] = fps
        emit("AE done")
        return ae.intermediate_path(output_dir)

    monkeypatch.setattr(ae, "render_sequence", fake_render)

    class Cfg:
        lrt_export_folder = str(tmp_path / "seq")
        output_path = str(tmp_path / "out")
        fps = 30

    msgs = []
    AEStage().run(Cfg(), msgs.append)
    assert called["seq"] == str(tmp_path / "seq")
    assert called["fps"] == 30
    assert any("AE" in m for m in msgs)


def test_pr_stage_emits_progress():
    msgs = []
    PRStage().run(config=None, emit=msgs.append)
    assert any("PR" in m for m in msgs)
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd timelapse-tool/python && .venv/bin/python -m pytest tests/test_stages.py -v`
Expected: FAIL（AEStage 仍是桩，不调用 `ae.render_sequence`）。

- [ ] **Step 3: 改实现**

编辑 `timelapse-tool/python/pipeline/stages.py`：把 `AEStage` 整个类替换为：

```python
class AEStage(Stage):
    name = "AE"
    manual = False

    def run(self, config, emit):
        from pipeline import ae
        ae.render_sequence(
            seq_folder=config.lrt_export_folder,
            output_dir=config.output_path,
            fps=config.fps,
            emit=emit,
        )
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd timelapse-tool/python && .venv/bin/python -m pytest tests/ -v`
Expected: 全部 PASS（注意：runner 等用桩 stage 的测试不受影响，因为它们用自定义 RecordingStage，不走真实 AEStage）。

- [ ] **Step 5: 提交**

```bash
git add timelapse-tool/python/pipeline/stages.py timelapse-tool/python/tests/test_stages.py
git commit -m "feat: AEStage 调用真实 AE 渲染编排"
```

---

## Task 5: 实机验证（需要真 AE 与真实序列）

**Files:** 无（验证任务）。**此任务必须在装有 After Effects 2026、有图形界面的本机执行；自动化代理应把此任务交回人类操作。**

- [ ] **Step 1: 准备一段真实的小图像序列**

用 LRTimelapse 导出（或手动放）一个含 20–50 张 JPG/TIFF 的文件夹，例如 `~/tl_test/seq/`，并建好输出目录 `~/tl_test/out/`。

- [ ] **Step 2: 单独验证 aerender 模板名**

Run: `"/Applications/Adobe After Effects 2026/aerender" -help` 并查看/在 AE「编辑 > 模板 > 输出模块」里确认是否存在名为 `Apple ProRes 4444` 的模板。
- 若存在：继续。
- 若不存在：在 AE 输出模块模板里新建一个用 QuickTime + Apple ProRes 4444 编码、命名为 `Apple ProRes 4444` 的模板；或记下实际可用名称，改 `pipeline/ae.py` 的 `PRORES_OM_TEMPLATE` 常量。

- [ ] **Step 3: 在 app 里跑到 AE 阶段**

启动 app（`cd timelapse-tool && export PATH="$HOME/.local/node/bin:$PATH" && npm start`），表单填 RAW 文件夹（任意已存在目录）、LRT 导出文件夹填 `~/tl_test/seq`、输出路径 `~/tl_test/out`、选导出预设，点开始 → 继续(BR) → 继续(LRT)。
Expected: 进入 AE 阶段后，AE 启动建工程，aerender 渲染，`~/tl_test/out/_ae_intermediate.mov` 生成，四阶段看板 AE 变绿、PR 变绿（PR 仍是桩）。

- [ ] **Step 4: 校验中间视频**

确认 `~/tl_test/out/_ae_intermediate.mov` 能正常播放、时长 ≈ 帧数 ÷ fps、分辨率与序列一致。
若失败，记录 aerender 终端输出，回到 `pipeline/ae.py` 调整（最可能是 OM 模板名或 `conformFrameRate`/导入序列的脚本细节），重跑。

---

## Self-Review Notes

- **Spec 覆盖**：实现第 5 节数据流的 AE 自动段（序列→ProRes 4444 中间视频，帧率随表单）。PR 消费中间视频在 2e。
- **类型一致性**：`AEStage.run` 用 `config.lrt_export_folder` / `config.output_path` / `config.fps`，与 `PipelineConfig` 字段一致；中间路径 `_ae_intermediate.mov` 由 `ae.intermediate_path` 统一派生，2e 将引用同一函数。
- **可测性**：`render_sequence` 注入 `run`/路径，纯函数（锚点/jsx/命令）单测；真实 AE 调用隔离在 Task 5 实机验证。
- **占位符扫描**：无 TODO。`PRORES_OM_TEMPLATE` 是明确的待实机确认常量，已在「关键约定」与 Task 5 标注。
- **已知实机风险**：aerender 的 ProRes 输出模板名、AE `-r` 运行 jsx 的行为、`importFile` 序列导入细节，都需在真 AE 上验证迭代——这是与外部软件集成的固有部分，已安排在 Task 5。
- **YAGNI**：不做多合成/字幕/转场；中间视频固定 ProRes 4444（最高画质，用完由 2e 决定是否清理）。
